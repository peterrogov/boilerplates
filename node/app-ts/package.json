{
  "name": "node-typescript-app",
  "version": "1.0.0",
  "description": "A boilerplate for an app made with node and typescript",
  "main": "src/index.ts",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "start": "npx ts-node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Rogov",
  "license": "MIT",
  "devDependencies": {
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}